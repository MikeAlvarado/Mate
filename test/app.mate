programa prueba {
    funcion origen {
        var x = 0;
        mientras (x < 10) {
            escribe(x);
            si (x % 2 == 0) {
                escribe("par");
            }
            pero si no {
                escribe("impar");
            }
            x = x + 1;
        }
    }
}